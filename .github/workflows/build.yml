name: Build Python Executables for Windows and Linux (deb)

on:
  push:
    branches:
      - main

jobs:
  build_windows:
    runs-on: windows-latest
    steps:
      - name: Check out the code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: Build Windows executable
        run: |
          python -m pyinstaller --onefile --name meu-ip --icon=assets/icone.ico --noconsole main.py

      - name: Upload Windows executable as artifact
        uses: actions/upload-artifact@v4
        with:
          name: windows-executable
          path: dist/meu-ip.exe  # Ajuste este caminho conforme necessário

  build_linux:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller
          sudo apt-get update
          sudo apt-get install -y ruby-dev build-essential
          sudo gem install --no-document fpm

      - name: Build Linux executable
        run: |
          python -m pyinstaller --onefile --name meu-ip --icon=assets/icone.png --noconsole main.py

      - name: Package executable into .deb
        run: |
          mkdir -p package/usr/local/bin
          cp dist/meu-ip package/usr/local/bin/
          fpm -s dir -t deb -n meu-ip -v 1.0 --prefix=/ package

      - name: Upload Linux .deb package as artifact
        uses: actions/upload-artifact@v4
        with:
          name: linux-deb-package
          path: package/*.deb

  release:
    needs: [build_windows, build_linux]
    runs-on: ubuntu-latest
    steps:
      - name: Check out the code
        uses: actions/checkout@v4

      - name: Download Windows executable
        uses: actions/download-artifact@v4
        with:
          name: windows-executable
          path: dist

      - name: Download Linux .deb package
        uses: actions/download-artifact@v4
        with:
          name: linux-deb-package
          path: package

      - name: Debug artifacts
        run: |
          echo "Conteúdo de dist:"
          ls -la dist/ || echo "Diretório dist não encontrado!"
          echo "Conteúdo de package:"
          ls -la package/ || echo "Diretório package não encontrado!"

      - name: Create GitHub release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}  # Usa a tag que acionou o workflow
          files: |
            dist/meu-ip.exe
            package/*.deb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
